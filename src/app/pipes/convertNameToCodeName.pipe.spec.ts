/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ConvertNameToCodeNamePipe } from './convertNameToCodeName.pipe';

describe('Pipe: ConvertNameToCodeNamee', () => {
  it('create an instance', () => {
    let pipe = new ConvertNameToCodeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
